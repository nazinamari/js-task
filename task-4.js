const products = [
	{ name: 'Apple', category: 'fruits', price: 2 },
	{ name: 'Carrot', category: 'vegetables', price: 1 },
];

const newProduct = { name: 'Banana', category: 'fruits', price: 3 };

// TODO: створити новий масив, який міститиме всі продукти + newProduct,
// але при цьому не змінювати оригінальний products.
// const updatedProducts = [...products, newProduct];

console.log(updatedProducts);

// * 💬 Варіації цієї задачі:

// * Оновити ціну продукту без зміни оригінального масиву:

const updatedProducts = products.map((p) =>
	p.name === 'Apple' ? { ...p, price: 3 } : p
);

// Тут спред { ...p, price: 3 } робить копію об’єкта і змінює лише одне поле.

// * Об’єднати два масиви продуктів:

const moreProducts = [{ name: 'Broccoli', category: 'vegetables', price: 2 }];

const allProducts = [...products, ...moreProducts];

// 🧩 Ключові навички, які перевіряє ця задача:

// Розуміння спред-оператора для масивів і об’єктів.
// Уміння працювати без мутації даних.
// Знання різниці між поверхневою та глибокою копією.
// Уміння оновлювати або об’єднувати дані “реактивно”.

// * 🧩 Що таке копія об’єкта або масиву?

// У JavaScript об’єкти та масиви зберігаються за посиланням (reference).
// Тобто змінна містить не самі дані, а “вказівник” на них у пам’яті.

// * 🔹 Поверхнева копія (shallow copy)

// Копіює тільки верхній рівень об’єкта або масиву.
// Вкладені об’єкти залишаються тими самими (за посиланням).

const user = {
	name: 'Alice',
	address: {
		city: 'Kyiv',
	},
};

// Створюємо поверхневу копію
const copy = { ...user };

copy.name = 'Bob'; // міняємо верхній рівень
copy.address.city = 'Lviv'; // міняємо вкладений рівень

console.log(user.name); // 'Alice' ✅ (незалежне значення)
console.log(user.address.city); // 'Lviv' ❌ (змінилось і в оригіналі!)

// Тому що copy.address і user.address — це той самий об’єкт у пам’яті.

// 🔸 Як зробити поверхневу копію
// Тип	Метод
// Масив	[...arr], Array.slice(), Array.from(arr)
// Об’єкт	{ ...obj }, Object.assign({}, obj)

// * 🔹 Глибока копія (deep copy)

// Копіює всю структуру об’єкта, включно з усіма вкладеними об’єктами.
// Після цього жодні зміни в копії не впливають на оригінал.

const users = {
	name: 'Alice',
	address: {
		city: 'Kyiv',
	},
};

// Глибока копія
const deepCopy = structuredClone(users);
// або const deepCopy = JSON.parse(JSON.stringify(user));

deepCopy.address.city = 'Lviv';

console.log(users.address.city); // 'Kyiv' ✅ (не змінилось)

// 🔸 Як зробити глибоку копію
// Метод	Особливості
// structuredClone(obj)	✅ сучасний стандартний спосіб (ES2021+)
// JSON.parse(JSON.stringify(obj))	працює майже завжди, ❌ але ламає функції, дати, undefined
// бібліотеки (lodash.cloneDeep(obj))	стабільний спосіб у великих проєктах

// * 💡 Порада для співбесіди:

// Якщо тебе спитають:
// “Яка різниця між поверхневою і глибокою копією?”

// Ти можеш відповісти:
// “Поверхнева копія створює новий об’єкт або масив,
// але зберігає посилання на вкладені структури.
// Глибока копія дублює всі рівні даних,
// тому зміни в копії не впливають на оригінал.”
