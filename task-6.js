// 🔹 forEach()
// 📘 Синтаксис:

array.forEach(function (element, index, array) {
	// Тіло функції
});

// Або зі стрілочною функцією:

array.forEach((element, index, array) => {
	// Тіло функції
});

// 📋 Параметри:

// element — поточний елемент масиву
// index (необов’язково) — індекс поточного елемента
// array (необов’язково) — сам масив

// 💡 Приклад:
const fruits = ['яблуко', 'банан', 'вишня'];

fruits.forEach((fruit, index) => {
	console.log(`${index + 1}: ${fruit}`);
});

// 📤 Вивід:

// 1: яблуко
// 2: банан
// 3: вишня

// 🔹 map()
// 📘 Синтаксис:

const newArray = array.map(function (element, index, array) {
	// Повертаємо нове значення
	return elementTransformed;
});

// Або зі стрілочною функцією:

const newArray1 = array.map((element, index, array) => {
	return elementTransformed;
});

// 📋 Параметри:

// element — поточний елемент масиву

// index (необов’язково) — індекс поточного елемента

// array (необов’язково) — сам масив

// 💡 Приклад:
const numbers = [1, 2, 3, 4];

const squares = numbers.map((num) => num ** 2);

console.log(squares); // [1, 4, 9, 16]

// 🧠 Головна відмінність:

// forEach() нічого не повертає (тільки виконує дію для кожного елемента).

// map() повертає новий масив, тому результат часто зберігають у змінну (const newArray = ...).

// 🔹 Приклад наочної різниці
const numbers2 = [1, 2, 3];

// forEach
numbers.forEach((num) => num * 2);
console.log(numbers2); // [1, 2, 3] — масив не змінився

// map
const doubled = numbers.map((num) => num * 2);
console.log(doubled); // [2, 4, 6] — створено новий масив

// ✅ Висновок:

// forEach — просто “проходимося” по елементах.

// map — трансформуємо масив і отримуємо новий.
